name: Deploy Node.js app to AWS

on:
  push:
    branches:
      - main
      - 'feature/**'
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - main
      - 'feature/**'

env:
  APP_NAME: first-app

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Cache node dependencies
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Show Node version
        run: node --version

      - name: Show Docker version
        run: docker --version

      - name: Build Docker image
        run: docker build -t $APP_NAME:${{ github.sha }} .

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Tag Docker image
        run: docker tag $APP_NAME:${{ github.sha }} ${{ secrets.DOCKER_USERNAME }}/$APP_NAME:${{ github.sha }}

      - name: Push Docker image to Docker Hub
        run: docker push ${{ secrets.DOCKER_USERNAME }}/$APP_NAME:${{ github.sha }}

      - name: Deploy to Hostinger VPS
        uses: appleboy/ssh-action@v1.1.0
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_PRIVATE_KEY }}
          script: |
            docker pull ${{ secrets.DOCKER_USERNAME }}/$APP_NAME:${{ github.sha }}
            docker stop $APP_NAME || true
            docker rm $APP_NAME || true
            docker run -d --name $APP_NAME -p 8585:8585 ${{ secrets.DOCKER_USERNAME }}/$APP_NAME:${{ github.sha }}
